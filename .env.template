# --- Database Passwords (CHANGE THIS IN YOUR .env FILE) ---
DB_ROOT_PASSWORD=supersecretpassword

# --- Database Connection Strings (uses the password above) ---
USER_DB_CONNECTION_STRING="server=mysql;port=3306;database=user_db;user=root;password=${DB_ROOT_PASSWORD}"
CATALOG_DB_CONNECTION_STRING="server=mysql;port=3306;database=catalog_db;user=root;password=${DB_ROOT_PASSWORD}"
LOAN_DB_CONNECTION_STRING="server=mysql;port=3306;database=loan_db;user=root;password=${DB_ROOT_PASSWORD}"